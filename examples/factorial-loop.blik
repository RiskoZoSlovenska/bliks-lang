let input 5
let output 6
let counter 7

===

func factorial
	set output 1
	set counter 0

	for counter < @input:
		mul output < @counter
	end
return

===

begin
repeat:
	pollnum input
	call factorial
	write @output
end
